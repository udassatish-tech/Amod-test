#include <iostream>
using namespace std;

int main() {
    int a, b, sum;

    cout << "Enter two numbers: ";
    cin >> a >> b;

    sum = a + b;

    cout << "Sum = " << sum << endl;

    return 0;
}

*************************************
#include <iostream>
using namespace std;

int main()
{
    int n;
    bool prime = true;

    cout << "Enter a number: ";
    cin >> n;

    if (n < 2)
        prime = false;
    else
    {
        for (int i = 2; i * i <= n; i++)
        {
            if (n % i == 0)
            {
                prime = false;
                break;
            }
        }
    }

    if (prime)
        cout << n << " is a prime number.";
    else
        cout << n << " is not a prime number.";

    return 0;
}

I have created my branch and added some programs to it. The first program calculates the sum of two numbers, and the second program checks if a number is prime or not.
